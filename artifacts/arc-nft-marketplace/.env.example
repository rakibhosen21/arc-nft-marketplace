# Optional: your thirdweb Client ID from https://thirdweb.com/dashboard
# Without this the app uses the default public key — fine for testing,
# but you should set your own for production to avoid rate limits.
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_client_id_here
